<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.chch.skj.auth_server.mapper.MemberDao">

<select id="selectUserUserIdAndPassword">
select uuid, userId, userPassword, `role`, createTime, expireTime, isDelete 
from t_user
where userId = "" and userPassword = ""; 
</select>

</mapper>